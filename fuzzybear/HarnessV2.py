
class HarnessV2():
	def __init__(self, binary):
		self.binary = binary
		
	
	def open_pipe(self, data):
		
		# with subprocess.Popen(
		# 			self.binary,
		# 			stdin  = subprocess.PIPE,
		# 			stdout = subprocess.PIPE,
		# 			stderr = subprocess.PIPE,
		# 			# preexec_fn = trace_me()
		# ) as pipe:
		# 	pipe.send_signal(SIGSTOP)
			# alive = pipe.poll()
			# Do coverage here
		pass